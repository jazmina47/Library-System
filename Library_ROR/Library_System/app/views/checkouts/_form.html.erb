<%= form_for(checkout) do |f| %>
  <% if checkout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(checkout.errors.count, "error") %> prohibited this checkout from being saved:</h2>

      <ul>
      <% checkout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date %>
    <%= f.datetime_select :date %>
  </div>

  <div class="field">
    <%= f.label :due_date %>
    <%= f.date_select :due_date %>
  </div>

  <div class="field">
    <%= f.label :checkout_id %>
    <%= f.number_field :checkout_id %>
  </div>

  <div class="field">
    <%= f.label :cardholder_id %>
    <%= f.number_field :cardholder_id %>
  </div>

  <div class="field">
    <%= f.label :staff_member_id %>
    <%= f.number_field :staff_member_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
