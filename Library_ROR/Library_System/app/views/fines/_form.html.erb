<%= form_for(fine) do |f| %>
  <% if fine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fine.errors.count, "error") %> prohibited this fine from being saved:</h2>

      <ul>
      <% fine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date_of_fine %>
    <%= f.date_select :date_of_fine %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.text_field :amount %>
  </div>

  <div class="field">
    <%= f.label :cardholder_id %>
    <%= f.number_field :cardholder_id %>
  </div>

  <div class="field">
    <%= f.label :fine_id %>
    <%= f.number_field :fine_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
